# local secrets / config
config.yaml

# runtime data
data/birdwatcher.db
data/captures/
data/library/
# generated Wikipedia "about" cache
data/species_facts.json
data/*.log
data/*.err
*.db

# models
*.pt

# go2rtc restreamer (binary + local config holding the camera URL)
go2rtc
go2rtc.yaml

# local dev preview server config
.claude/launch.json

# container compose overrides (local port/volume tweaks)
docker-compose.override.yml

# downloaded reference images (regenerate via tools/fetch_reference_images.py)
assets/reference/
assets/_mock_*.txt
assets/*.log

# python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# os
.DS_Store
Thumbs.db
