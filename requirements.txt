# --- core (always needed) ---
flask>=3.0
pyyaml>=6.0

# --- capture + detection (milestones M2-M3) ---
opencv-python-headless>=4.9
numpy>=1.26
ultralytics>=8.1            # YOLO bird detector (auto-downloads yolov8n.pt)

# --- species ID (milestone M4) ---
# bioclip backend (local zero-shot vs the catalog — CHOSEN, no API key):
open_clip_torch>=2.24
pillow>=10.0
#
# tfhub backend (local; no Python 3.14 wheels yet):
# tensorflow>=2.15
# tensorflow-hub>=0.16
#
# claude backend (API, needs ANTHROPIC_API_KEY):
# anthropic>=0.40
